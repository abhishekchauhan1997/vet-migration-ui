import Layout from "../layout/Layout";

import AddClient from "../Pages/PostCard/AddClient";

const AboutPage = () => {
  return (
    <Layout>
      <AddClient />
    </Layout>
  );
};

export default AboutPage;
